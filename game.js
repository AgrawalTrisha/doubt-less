var config = {
  width: 360,
  height: 272,
  scale: {
    parent: 'yourgamediv',
    mode: Phaser.Scale.NONE,
    zoom: 2.5,
  },
  backgroundColor: 0xDA5E53,
  scene: [],
	pixelArt: true,
}

var game = new Phaser.Game(config);